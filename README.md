# stackingrockss GitHub defaults

This repository stores default GitHub community health files and issue templates for `stackingrockss` repositories.

Repo-local templates override these defaults. Keep shared workflow fields here unless a repository needs its own specialized template.
